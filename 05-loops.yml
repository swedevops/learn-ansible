- name: demo on loops
  hosts: localhost
  connection: local
  tasks:
    - name: Demo tasks on loops
      ansible.builtin.debug:
        msg: fruit name - {{ item }}
        loop:
          - mango
          - kiwi
          - banana
